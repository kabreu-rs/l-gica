import java.util.Scanner;

public class Ecriarcont {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);

        int contador = 0;
        int x = 2;

        System.out.println(" O  1° valor é " + x + " e o 2° é " + contador);

        contador = contador + x;

        System.out.println(" O valor de " + contador);
        contador = contador + x;
        contador += x;
        contador += x;
        contador += x;
        contador += x;
        contador += x;  
        contador += x;
        contador += x;
        contador += x;
        contador += x;  
        System.out.println(" O valor de " + contador);
    }
    
   


    
}
